# Вопросы команде (чеклист для созвона / чата)

Скопируй нужный блок в чат. Связанные карточки: [#94](https://trello.com/c/NCWUFlqw) Done · [#95](https://trello.com/c/T28F0b7e) To Do.  
Контекст: [ОТЧЁТ-2026-09-26.md](./ОТЧЁТ-2026-09-26.md) · [AUTH-INTEGRATION.md](./AUTH-INTEGRATION.md).

---

## A. Владу / Auth-команде (Backend-client) — приоритет P0

Карточка: [#95](https://trello.com/c/T28F0b7e)

1. **Какой claim содержит UserId** в access JWT — `sub`, `nameid`, `userId` или другое?
2. **Формат UserId** — Guid string (`xxxxxxxx-xxxx-…`)?
3. **Роль:** имя claim (`role` / `roles` / полный URI) и значения (`User` / `Admin`)?
4. **Issuer** и **Audience** access token (точные строки).
5. Чем валидировать подпись: **HS256 shared secret** или **RS256/JWKS**? Куда смотреть ключ (secret name / URL JWKS)?
6. Есть ли в access token **name** и **email**? (нужно для RecipientName в заказах без Internal API)
7. Как Product API получает **Internal JWT** для `GET /internal/users/{id}` (endpoint service-login, permissions `users.read`)?
8. **CORS:** разрешён ли origin витрины `http://localhost:3000` и prod front?
9. Пример успешного ответа `POST /api/auth/login` (JSON: поля токена и user) — чтобы выровнять FE-маппинг.
10. Register: обязателен ли полный flow `register → verify-email → complete-registration`, или для демо есть упрощённый путь?

---

## B. Яне / фронт-команде (архитектура) — приоритет P0

Контекст: претензия «код без архитектуры, которую заложили с Яной».  
Эталон: [perry-admin-front](https://github.com/ITSTEP-PERRY/perry-admin-front) · заготовка витрины: ветка [`alex`](https://github.com/ITSTEP-PERRY/perry-front/tree/alex) · наш рабочий порт: [`feature/figma-storefront-port`](https://github.com/ITSTEP-PERRY/perry-front/tree/feature/figma-storefront-port).

1. **Источник правды для витрины до защиты:** оставляем Figma-порт (Context + CSS + `router.tsx`) или переезжаем на архитектуру Яны (Ant + Redux + `Components/` / `forms/` / `widgets/`)?
2. Если оставляем порт — можно ли **вычистить** из `main` мёртвую линию Ant/Redux (`pages/HomePage/`, дубли `Components`, store), чтобы не мешала?
3. Если берём линию Яны — кто и к какому сроку **переносит** уже готовые экраны (каталог, PDP, Account, admin) в эту структуру?
4. **`perry-admin-front` vs `/admin` в perry-front:** какой admin показываем на защите? Один репо или два?
5. Есть ли **короткий гайд/ADR** по архитектуре с Яной (именование папок, где класть страницу/форму/API)? Если нет — кто напишет 1 страницу?
6. Куда класть PR по витрине: только `feature/*` с ревью Яны, или можно в `feature/figma-storefront-port`?
7. Ветки `feature/home-page`, `feature/product-list-categories`, `alex` — какие ещё живые, какие архив?

---

## C. Product / бэкенд-команде

1. Мерж `#94` (`DropUserTables`) в `main` — кто и когда? Нужен ли даунтайм / бэкап БД перед миграцией?
2. Старые строки `Orders.UserId`, которых уже нет в Auth — ок orphan Guid или чистим?
3. Нужен ли Product’у вызов Internal Auth для имён в admin-orders, или хватит `RecipientName` с checkout?
4. Локальный `AuthTokens` (#15 OTP в Product БД) — оставляем для forgot/notify или тоже уезжает в Auth?
5. `#93` admin orders stats всё ещё в Review — автор предложения подтвердил контракт?

---

## D. Процесс / Trello / защита

1. Кто мержит feature → `main` (front и back) — по-прежнему «другой человек»?
2. `#92` слайды — кто владелец?
3. Mobile `#79`/`#81` — не трогаем; актуальный статус?
4. На демо: какие URL называем (local / Azure Auth / Azure Product)?
5. Нужна ли одна общая страница «как устроены репо» в org README / wiki?

---

## E. Что можно ответить команде прямо сейчас (черновик сообщения)

> По #94: из Product API убрали Users/FK, остался UserId из JWT. Логин/регистрация/админ-users — на Auth Влада. Карточка #95 — список вопросов по claims.  
> По фронту: согласен, что в perry-front сейчас две линии (наш Figma-порт на Context/CSS и архитектура Ant/Redux как в perry-admin-front / ветка alex). Рабочая витрина на защите сейчас — порт. Предлагаю на созвоне выбрать один source of truth и вычистить вторую линию из main, либо запланировать перенос экранов в архитектуру Яны. Документ: docs/ОТЧЁТ-2026-09-26.md и docs/СВЕСТИ-ДВЕ-ЛИНИИ.md.
