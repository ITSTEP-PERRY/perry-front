# Изменения Perry — 19.09.2026

Документ фиксирует работу **спринта к защите** за 19.09.2026: React-витрина + React-админка + API, без редизайна (сохраняем `admin-shell` / `ap-*` / storefront-токены).

Репозитории:
- Frontend: https://github.com/ITSTEP-PERRY/perry-front (`feature/figma-storefront-port`)
- Backend: https://github.com/ITSTEP-PERRY/Back_end_for_our_poroject (зеркало `Teslyar75/My_Amazon2`)

Скриншоты дня: [screenshots/sprint-2026-09-19/README.md](./screenshots/sprint-2026-09-19/README.md) (**15** кадров).

Доска: [ITSTEP-PERRY](https://trello.com/b/bwEYs3Kq/itstep-perry) — закрыты карточки блоков A–D / To Do (#34, #36–#40, #65–#69, #77/#78/#80, #29/#30, #22, #5, #76, #14 и др.).

---

## 1. Витрина React (PDP + демо-дыры)

### Reviews (#34)
- Сортировка: Most recent / Highest / Lowest
- Фильтр по звёздам и frequent tags
- Helpful (счётчик), See more (по 3), Translate (demo toast)
- Create review → API (ранее #44)

### Инфо-модалки PDP (#36–#40)
Один компонент `PdpInfoModal` (chrome как Account `acc-modal`):
- Delivery, Payment methods, Security, Returns, About seller
- Открытие из buy-box (кнопки ›)

### 404 (#83)
- `NotFoundPage` / product not found в стиле витрины

### Notify when available (#29 + #30)
- Кнопка на PDP при `OutOfStock`
- `POST /api/products/{id}/notify` + таблица `StockNotifyRequests`
- Письмо через `IEmailSender` (stub по умолчанию)

### Footer + back to top (#22)
- Уже в `AppShell` (Support / Legal / Social + to-top)

---

## 2. React-админка (блок C)

Только React (`:3000/admin/*`), Razor UI не трогали для новых экранов.

| # | Экран | Что сделано |
|---|--------|-------------|
| **#65** | Categories | Roots/sub list, create/edit, deactivate/activate, `includeInactive` |
| **#66** | Products | Список + preview; create/edit форма `ap-edit` |
| **#69** | Reviews | Новая страница + API модерации Hide/Approve/Delete |
| **#68** | Users | Фильтры Active/Deleted/All + role, soft-delete, restore |
| — | Orders | Список + панель деталей / смена статуса (существующий экран) |

Правка UX: длинные email/имена в Users — ellipsis (без наложений колонок).

---

## 3. Backend API

- `AdminReviewsController` — `/api/admin/reviews`
- `StockNotifyController` — `/api/products/{id}/notify` + миграция `AddStockNotifyRequests`
- Categories: `?includeInactive=true` для Admin
- Users: `status` / `role` фильтры, `POST /users/{id}/restore`
- Swagger (#76): описание, теги, Bearer JWT, CustomSchemaIds
- Docker (#5): `docker-compose.yml`, `.env.example`, [docker.md](./docker.md)
- SMTP (#14): stub по умолчанию; гайд [SMTP-НАСТРОЙКА.md](./SMTP-НАСТРОЙКА.md) + речь [РЕЧЬ-SMTP-ДЕМО.md](./РЕЧЬ-SMTP-ДЕМО.md)

---

## 4. Mobile (#77 / #78 / #80)

Адаптив уже был в React (`useIsMobile`, `@media` в storefront/auth). Карточки перенесены в **Done** без повторной переписки UI.

---

## Проверка

```bash
# API
cd My_Amazon2
dotnet run --project src/Perry.Api --launch-profile http
# → http://localhost:5272/swagger

# Frontend
cd D:\Perry
npm run dev
# → http://localhost:3000
# Admin: /admin/login → Admin / Admin
```

Smoke: [SMOKE-ЗАЩИТА.md](./SMOKE-ЗАЩИТА.md).

---

## Связанные документы

- [ПОРЯДОК-ЗАДАЧ-СПРИНТ.md](./ПОРЯДОК-ЗАДАЧ-СПРИНТ.md)
- [ИЗМЕНЕНИЯ-Account-2026-09-17.md](./ИЗМЕНЕНИЯ-Account-2026-09-17.md)
- [ИЗМЕНЕНИЯ-2026-09-17.md](./ИЗМЕНЕНИЯ-2026-09-17.md)
