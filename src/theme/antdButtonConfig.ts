import type {ButtonConfig} from "antd/lib/config-provider/context";

export const buttonConfig: ButtonConfig = {
    style: {
        letterSpacing: "2%",
        padding: "8px 32px",
        borderRadius: 4,
        fontSize: 16,
    }
}
